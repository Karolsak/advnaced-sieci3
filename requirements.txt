# Requirements for DC Motor Braking Multi-Physics Simulator
# Python 3.7 or higher required

# Core scientific computing
numpy>=1.21.0
matplotlib>=3.4.0
scipy>=1.7.0

# Note: tkinter usually comes pre-installed with Python
# If tkinter is missing, install it via your system package manager:
#   Ubuntu/Debian: sudo apt-get install python3-tk
#   Fedora: sudo dnf install python3-tkinter
#   macOS: should be included with Python from python.org
#   Windows: included with standard Python installation
